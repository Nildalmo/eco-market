"use client";

import Providers from "@/app/providers";
import Footer from "@/components/Footer";

export const metadata = {
  title: "Lojas",
  descrption: "Generated by Next.js",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-br">
      <body>
        <Providers>
          {children}
          <Footer />
        </Providers>
      </body>
    </html>
  );
}
